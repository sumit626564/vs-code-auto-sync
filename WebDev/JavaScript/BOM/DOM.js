let heading = document.getElementById('greet');
console.log(heading);

//! ! innerHTML : change the HTML of an element
heading.innerHTML = '<i>Good Evening....</i>'

// ! innerText: Change the content
heading.innerText = '<i> Good'
